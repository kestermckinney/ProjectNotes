# Native email build progress

Source revision: `0507b5393a4e878e85d2079f568519c7e29a6d13` (2026-09-11, `feature/email`). Evidence is synthetic and contains no account data, personal addresses, report exports, tokens, or filesystem locations.

| Task | Status | Evidence / remaining work |
| --- | --- | --- |
| T00 — inventory, fixtures and capability ledger | in progress | Inventory and first pure-builder capture added. Broaden synthetic matrix and capture renderer metadata before G1 can pass. |
| T01 — libraries, value contracts and test harness | verified | Three static target boundaries, stable enum/value contracts, async seams, queued-type registration, and focused fake-backed contract tests are in place. |
| T02 — settings scope, artifact store and operation recovery | verified | Profile/database-scoped settings and atomic operation staging/recovery pass the focused test matrix. Reused operation IDs are rejected before they can replace a recoverable manifest or orphan its staged artifacts, a generated path can be registered only once per operation, and publication rechecks recorded size/hash integrity. |
| T03 — read-only communication snapshots | in progress | Dedicated read-only SQLite loader covers project/team/company/notes data, project report-period/EVM fields, status activities, full tracker records/comments, and canonical content/audience fingerprints; selected-note ownership and attendee provenance are verified. Meeting-attendee candidates are limited to the request's selected notes (or all notes only for the explicit project-wide meeting report), and deleted people cannot reappear through attendee rows. It requires the shared lock, reads in a transaction, captures manager/company inputs in the request, and has a single-worker async repository that returns on its owner thread, waits behind the shared writer lock, drops cancelled requests, and rejects stale database generations. The desktop controller now scopes an instance to each successfully opened database, increments its generation, and joins its worker before closing the shared database. Handoff reloads the source and compares its fingerprint before any backend request. Broader relations and action/controller composition remain. |
| T04 — audience resolution and recipient model | in progress | Pure source/company resolver, safe overrides, recipient-model edits/reset, and database/workflow/project-scoped rule preset persistence exist. Project-scoped presets cannot be saved as global or another project’s default. Manual recipient input rejects ambiguous/header-injected addresses, case-insensitive duplicates, and invalid roles; role conflicts on source duplicates are diagnosed without Bcc promotion, and resolved selections carry the explicit address-later choice. A source change clears that explicit choice and all manual entries instead of silently carrying them to a different audience. Empty sources produce non-blocking diagnostics that persist through recipient edits. The model exposes persisted-person overrides for controller-only preset application; manual rows and address-later remain review-local. Current-selection recipient IDs are distinct from selected note IDs, so that source resolves the captured workflow recipient context rather than impossible note/person comparisons. Explicit project-team membership prevents an external meeting attendee from silently becoming a team recipient. The full source/company scenario matrix remains. |
| T05 — field catalog, parser and legacy replacement | in progress | Literal fields-v1 parser rejects unresolved/reserved/call-like tokens, honors optional-field metadata, and enforces its catalog/workflow scope. HTML-mode expansion escapes inserted field values; template storage rejects empty or CR/LF-injected subjects before persistence, and expanded subjects are revalidated so snapshot data cannot inject mail headers. Protected native content is now inserted between independently rendered user segments rather than through a replaceable sentinel, so literal field data cannot duplicate/inject report HTML. Legacy-v1 now traverses typed one-based table rows for repeated substitutions without code evaluation, retains its required three-part fallback behavior, and rejects overlong paths rather than silently truncating them. Rich blocks and the full legacy context mapping remain. |
| T06 — optional computed fields and G5 review | in progress | A fresh-engine bounded scalar evaluator supports both expression and documented `function compute(context, helpers)` forms, exposes copied/deep-frozen structured context (with legacy flat-key compatibility), and implements the published 64-definition, 64 KiB UTF-8 output, 1 MiB UTF-8 context, 250 ms per-field, and 2 s total limits. It rejects invalid/non-scalar or duplicate output names and has pre-start/in-flight cancellation plus timeout tests. `ComputedFieldService` now runs that evaluator on its own bounded worker pool, rejects duplicate operation IDs, exposes cancellation, and returns completions to its owner thread. A reproducible Qt rich-text probe preserves basic formatting and strips script markup but retains external image URLs, so it is insufficient by itself as the G5 imported-prose policy. Imported-script approval/activation and parser/review policy selection remain. |
| T07 — template/default migration and storage | in progress | Database-scoped JSON user revisions overlay resettable native defaults for all four workflows; workflow and database changes clear stale selections so each new scope shows its applicable baseline. Every legacy dynamic Email/Meeting row imports idempotently into a review-only preserved-definition ledger with stable occurrence identity, so identical source rows are retained separately; only the unambiguous legacy Email + `project_notes` context is also listed under Send Meeting Notes, but its retained legacy origin blocks application until the G5 import-review policy is explicitly approved. Incomplete template drafts persist with diagnostics (but header injection is rejected); application/handoff remains the enforcement boundary. Nonempty malformed JSON in either user-template or preserved-legacy storage is explicitly corrupt and is never overwritten by import/save. An optional opaque audience-preset reference persists with a template without saving recipients or modifying audience state. Legacy report ExportSubFolder values now migrate only into missing native settings. Conflict UI and per-mode envelopes remain. |
| T08 — native meeting-note content builders | in progress | Separate Send Meeting Notes and project-wide Meeting Notes Report builders now produce typed documents from the snapshot. The project report applies reporting-date/internal filtering, A4 margins, legacy meeting-block/title/action-table/footer/branding structure, and native subject/stem conventions; its generation-bound preparation factory now feeds the native project review route. Full legacy layout/golden parity remains. |
| T09 — status calculations and formatting | in progress | Pure review-period/EVM helpers and focused tests pass. Explicit Python float-rounding boundaries (2.675, 2.685, 2.695, and negative 2.675) now match the native formatter; the broader legacy parity matrix remains. |
| T10 — tracker and status report builders | in progress | Native Status and Tracker builders now produce typed HTML/fragment/plain-text output. Both exclude internal tracker/issue items from external reports and include them in internal reports. Tracker applies type/status/internal filters plus legacy grouping and priority/due-date sorting; its shared option validator rejects absent or empty type/status filter sets rather than relying on the QML caller. Empty tracker output now spans the correct 12/13 columns for external/internal layouts. Status includes activity/issue/stakeholder/review-period/EVM sections. `ReportService` dispatches all four native builder inputs, and a typed project-report preparation factory now maps persisted snapshots to Status/Tracker documents plus their native default audiences. Full option/action UI and legacy parity remain. |
| T11 — WebEngine renderer and publication | implemented; runtime verification deferred | Fedora Qt 6.11.2 WebEngine development/runtime packages are now installed. `WebEngineReportRenderer` has a GUI-thread `QWebEnginePage` lifecycle, explicit A4 layout, absolute caller-owned output path, non-persistent restricted profile, cancellation, 30-second timeout, crash detection, and one-completion guard. Its explicit request interceptor allows only the renderer's `about:`/`data:` main frame and blocks all remote, file, qrc, nested-frame, and data subresources. `ArtifactStore` can reserve an unregistered name in the exact operation artifact directory and register/hash only a completed regular file below that directory; focused traversal/publication coverage passes. The desktop composition root owns the renderer; all initial review and template-rebuild paths wait for asynchronous PDF staging before entering/replacing review. A new render cancels its predecessor and ignores stale completion by operation ID. The rendering module and full desktop target build against declared WebEngine Core/Quick dependencies; WebEngine Quick initializes before `QApplication`. `ldd` confirms the desktop executable resolves `/lib64/libQt6WebEngineCore.so.6` and `/lib64/libQt6WebEngineQuick.so.6`; startup no longer redirects to PyQt wheel helper/resources. The normal local desktop suite passes 8/8. **Return item:** run the opt-in `ENABLE_WEBENGINE_RENDERER_TESTS` target plus package smoke/PDF baseline checks on a native/package-like host; this container’s Chromium helper aborts at sandbox shutdown even with `QTWEBENGINE_DISABLE_SANDBOX=1`, so G4 remains unverified by explicit maintainer decision. |
| T12 — preparation controller and dispatch state machine | in progress | `EmailService` now selects registered backends, enforces one active operation, sends cancellation only to the active backend, and ignores stale callbacks. `EmailContentBuilder` creates immutable requests with explicit inline/HTML attachment/PDF attachment/no-email semantics, including final nonempty/header-safe subject validation plus recipient syntax/role/duplicate checks after review edits. `CommunicationsController` now owns a single preparation revision, blocks edits while dispatch is pending, ignores stale lifecycle results, preserves failures for review, and completes no-email without a backend. The desktop composition root owns one `EmailService`, `CommunicationsController`, and recipient model for the application lifetime and exposes the latter two to QML. Native note and project-report reviews both retain their prepared value; before any backend handoff, the controller freezes review edits while the desktop reloads the snapshot and compares its fingerprint. A changed or unreadable source returns a review validation failure rather than silently sending changed content; its controller gate also rejects direct handoff until a regenerated preparation replaces it. Unchanged source retains review-local subject/body/attachments and only refreshes recipients/address-later. Cancelling revalidation immediately cancels and clears the matching repository operation, so stale work cannot hold up a later deliberate handoff. It registers no backend or menu action. Template/artifact orchestration, account checks, action/QML wiring, and recovery remain. |
| T13 — extract Office 365 service | in progress | `MicrosoftOAuthManager` was moved from File Finder into `ProjectNotesIntegrations`, which is declared before and linked by File Finder. It accepts a fakeable HTTP transport while retaining the production Qt-network default. Developer profiles use a qualified credential key while the production profile retains its historic tenant/client key. Contracts now cover restoration, explicit sign-out, and invalid-grant refresh invalidation/removal without a network call, as well as the token-free facade. Existing device-code scopes and File Finder behavior are unchanged; broader provider-error coverage remains. |
| T14 — feature scopes, account identity and token-free facade | in progress | OAuth now takes explicit requested scopes (legacy File Finder scopes remain the default), preserves a valid File Finder access token while an email-consent request is pending, and increments session generation on a confirmed capability/configuration/sign-out change. Device-code sign-in and refresh restoration are single-flight, preventing duplicate consent requests; a background refresh cannot clear pending interactive consent. `Office365Service` is a token-free facade that injects authorization internally, reports account generation/authentication and only token-response-confirmed granted scopes (not merely requested ones), and generation-guards a `/me` label refresh. Confirmed scope metadata persists separately from tokens and clears on invalidation. The Settings facade exposes a separate email-draft consent action, with neither Mail.Send nor calendar/Teams/Files scope. Fake tests cover draft-only scope requests and preservation before new consent completes. Denied-consent, account-switch, and real-account validation remain. |
| T15 — Graph draft adapter | in progress | `GraphEmailBackend` creates only `/me/messages` drafts through the token-free service, preflights the confirmed `Mail.ReadWrite` grant plus account generation/headers/recipients/files, and rechecks each staged attachment's regular-file identity, size, and SHA-256 before draft creation. It uploads operation-owned simple attachments sequentially and streams larger files through Graph upload sessions in bounded 320 KiB chunks without sending OAuth credentials to the returned upload URL. Cancellation is checked at each async transition, including after draft creation and before the first upload PUT; a transport failure after the draft POST and all post-create cancellations/failures retain uncertain outcomes rather than inviting a duplicate create. A Graph `webLink` is retained only when it is an approved HTTPS Outlook host, remains inert result presentation metadata, and is never launched by the backend. Broader provider-error coverage remains. |
| T16 — mailto and manual fallback | in progress | The mailto adapter performs strict RFC-style percent encoding, capacity/content preflight, recipient/header validation, injected opener handoff, and reports launch as unconfirmed. A per-operation one-shot guard records the attempt before platform launch, preventing duplicate compose windows even if platform outcome is uncertain. Native review now exposes an explicit Save generated attachment action: only an operation-owned app-generated artifact can be copied to a user-selected destination; user-supplied attachments cannot be exported through it. No delivery action is enabled. |
| T17 — Outlook Classic helper | in progress | The shared parent/helper boundary now has strict, versioned, bounded NDJSON request and response frames. A validated immutable `EmailRequest` produces the typed prepare payload with role-preserving recipients, staged artifact identities/paths, subject/body data, and no credentials; invalid roles cannot be serialized as ambiguous empty helper values. Its prepare boundary requires an exact operation-owned directory and rejects body/attachment paths outside it, including lexical escapes and sibling prefixes. Responses correlate operation identity with staged/presented/cancelled/failed state, outcome certainty, opaque item handle, and error code. Both encoding and decoding reject malformed frames, unknown enum values, version mismatches, missing IDs, invalid certainty, and oversize messages on every host. The Windows STA COM helper, process lifecycle, and Windows evidence remain. |
| T18 — Thunderbird compose adapter | in progress | A shell-free injected launcher builds validated `-compose` arguments with To/Cc/Bcc, escaped subject, UTF-8 body-file URL, and attachment URLs. Its compose boundary now accepts body/attachment files only after canonical containment beneath the supplied manifest-owned operation directory, rejecting unreadable paths and symlink/lexical escapes. Executable selection is now an injectable, ordered probe: a valid explicit setting wins, duplicate candidates are probed once, and it neither invokes a shell nor resolves from PATH. Discovery UI/candidate policy and platform/sandbox evidence remain. Thunderbird behavior tests remain held by maintainer direction. |
| T19 — macOS Mail bridge | in progress | `MacMailBackend` now gives every host an explicit adapter boundary. Non-Apple builds report `mac-mail-unavailable`, and cancellation is retained as a certain cancelled result; Apple builds report the bridge unavailable until a proven Objective-C++ implementation is supplied. The signed macOS bridge, automation/file-access handling, and G3 evidence remain. |
| T20 — report/options and email preparation QML | in progress | `MeetingNotesReviewDialog` provides a native review surface for the saved note path, and the project page’s Generate Report menu routes Meeting Notes, Status, and Tracker Items reports through one native review dialog with reporting-date/internal options, tracker type/status filters, email mode, generated subject/plain-text preview, safe subject editing, recipient edits, and a file chooser for staged user attachments. Inline-HTML reviews provide an explicit review-local HTML source editor; attachment modes remain read-only so they cannot diverge from their manifest-owned artifact. All three report workflows can select a protected-content template. Each review owns an operation manifest: HTML-attachment mode stages the native standalone document, while selected files are copied there before review lists them; PDF mode now enters review only after native asynchronous, operation-owned PDF staging completes. These views are model-backed and expose Graph draft handoff only after the selected backend, verified identity label, and confirmed least-privilege scope are present. Preview/recovery coverage and deferred native-runtime verification remain. |
| T21 — audience picker QML | in progress | The reusable recipient pane now appears in native review dialogs, with C++-backed selection/To-Cc-Bcc/manual-address/add/remove/reset controls, live unique role/count totals, company-grouped eligible checklists, source provenance, and a collapsible exclusion list with resolver-owned reasons. Shortcuts for project team/meeting attendees/status recipients and company refinements, unknown-company opt-in, and manager exclusion pass to the C++ resolver against the current immutable snapshot. A review can save its active rule and persisted-person overrides as global or project-scoped presets, reapply eligible saved presets, and set the selected preset as a global or project default. An eligible default is reapplied after asynchronous review preparation. Manual rows/address-later are intentionally not persisted. Internal-report mismatch disclosure and the full scenario matrix remain. |
| T22 — template editor and settings QML | in progress | The desktop Settings page now exposes a profile/database-scoped native template editor. It offers workflow-scoped literal field insertion, rich and optional plain-text body drafts, validate/save/duplicate/reset actions, and displays parser diagnostics without QML evaluation. A protected `{{ content.body }}` block is now required when custom rich/plain prose is supplied; all four native workflows have resettable wrapper defaults and report/note review can apply saved templates around that block without exposing report markup to ordinary field interpolation. It also exposes a workflow-matched, global audience-preset reference; this is deliberately reference-only, while application remains an explicit review action with project context. Preferred-backend and Thunderbird-path settings now bind to `EmailSettingsStore`, but they explicitly do not activate delivery until the selected adapter supports the prepared content. Computed-field activation/import review and remaining adapter discovery are still incomplete. |
| T23 — native action routing and end-to-end desktop wiring | in progress | Save-before-action now routes the project’s Generate Report menu through persisted-ID, generation-bound snapshot preparation and review for Meeting Notes, Status, and Tracker Items reports; saved note review remains its own native action. The routes retain the selected mode/backend as immutable preparation metadata. The application root registers only the Graph draft adapter, and the review reveals its action only after Graph is selected with confirmed draft scope and identity label; the guarded source-revalidation path remains mandatory. Exact migrated bundled report callbacks are suppressed during plugin coexistence while custom/unrelated actions remain. Intended menu contexts, adapter fake-handoff coverage, and shutdown/database-switch review remain. |
| T24–T27 | in progress | Native Qt WebEngine package verification is now executable: the Windows deploy target fails unless its own helper/resources/locales are staged (not merely PyQt wheel files), macOS release builds run the equivalent bundle check, and the Flatpak build script verifies the matching `/app` paths. The macOS signer applies Qt's supplied Chromium-helper entitlements to `QtWebEngineProcess.app` rather than the application's Python-oriented entitlement file. Widgets is explicitly unsupported for future releases, so no native bridge or further Widgets changes are required; its historic Python plugin workflow is outside this release scope. A new in-app user-guide page documents the native report/review paths without claiming email-client delivery before it is certified, and preserves legacy plugin help. Package smoke evidence remains deferred by the explicit runtime decision. An exact, non-destructive manifest identifies only the three native-replaced bundled report plugins for future G6 cleanup. No plugin files, user plugins, settings, or data have been deleted. |

## Environment and build availability

- CMake 4.3.0; Qt 6.11.2; Python 3.14.7 with PyQt6 installed.
- On the current Fedora workstation, CMake discovers `/usr/lib64/ccache/c++` first and its pthread configure probe fails unless ccache is disabled. Configure and build with `env CCACHE_DISABLE=1 cmake ...`; this has been confirmed to find Threads, Qt, Python, Hunspell, and OpenSSL successfully. Keep builds serial (`-j1`) while memory pressure is being investigated.
- Existing `build-qmltests` is Unix Makefiles / Debug and has Qt6 at `/usr/lib64/cmake/Qt6`. `cmake --build build-qmltests --target help` listed `tst_filefinder` and `tst_qmldesktop`.
- Direct import of a legacy report plugin aborted while its Qt WebEngine dependency initialized in this container, even with `QT_QPA_PLATFORM=offscreen` and `QTWEBENGINE_DISABLE_SANDBOX=1`. No legacy workflow or external action ran. The test-only AST capture tool avoids that import and is the current safe baseline route.

## Legacy inventory

| Workflow | Legacy entry point / content path | Default email mode | Subject / filename stem | Recipient source |
| --- | --- | --- | --- | --- |
| Send Meeting Notes | `base_plugin.py::menu_send_notes` → `includes/noteformatter.py::NoteFormatter` | formatted inline HTML | `{project} {name} - {note date} {title} Notes` | `Full Project Team` |
| Tracker Items Report | `exporttrackeritems_plugin.py::TrackerItemsExporter` | inline HTML | `{project} {name} - Tracker Items {date}` / `Tracker Items` | exported context (currently passed as empty invitee label) |
| Status Report | `exportstatusreport_plugin.py::StatusReportExporter` | inline HTML | `{project} {name} - Status Report {date}` / `Status Report` | `Receives Status` |
| Meeting Notes Report | `exportnotes_plugin.py::NotesExporter` | inline HTML | `{project} {name} - {date}` / `Meeting Minutes` | exported context (currently passed as empty invitee label) |

Tracker initializes Tracker Items, New, and Assigned selected; Action Items, Resolved, Defered, and Cancelled unselected. All report forms initialize Display, Internal, and Generate HTML unchecked; the inline-HTML radio button is checked. Status and meeting reporting dates are set to the execution date. Report filename suffix is ` Internal` for internal output. Export-subfolder settings are `Export Tracker Items`, `Export Status Report`, and `Export Meeting Notes` / `ExportSubFolder`.

## Fixture and capture evidence

- Fixture: `tests/desktop/fixtures/email/synthetic-project.json`.
- Tool: `tests/desktop/fixtures/email/capture_legacy_baselines.py`; it produces the four HTML baselines in a caller-owned directory and prints SHA-256, subjects, and recipient labels. It only compiles pure legacy builders from source; it cannot call a collaboration/process/network path.
- Capture command/result: `python3 tests/desktop/fixtures/email/capture_legacy_baselines.py --output <temporary directory>` passed on 2026-09-12. It generated all four sanitized HTML documents (24,594 / 4,232 / 10,680 / 2,638 bytes respectively) and the frozen subjects, recipient labels, and SHA-256 values in `baseline-summary.json`.
- Reproducibility check: the capture output was byte-for-byte equal to `baseline-summary.json`; `python3 -m py_compile tests/desktop/fixtures/email/capture_legacy_baselines.py` passed.
- Reproducibility refresh: `python3 tests/desktop/fixtures/email/capture_legacy_baselines.py --output <temporary directory>` regenerated all four synthetic documents on 2026-09-15. Their SHA-256 values exactly match `baseline-summary.json`; no plugin runtime, renderer, client handoff, network, or account path was invoked.
- Focused existing build/test: `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_filefinder -j2` passed. (The initial build attempt hit a read-only ccache location.) `env QT_QPA_PLATFORM=offscreen build-qmltests/tests/desktop/tst_filefinder -maxwarnings 0` passed: 8 passed, 0 failed. `ctest --test-dir build-qmltests -R '^tst_filefinder$'` found no registered tests, so the executable was run directly.
- Renderer metadata: pending. Legacy plugins construct `QPageLayout`/`QPageSize` during dialog construction, which cannot safely occur in this headless Qt WebEngine environment; capture must be repeated on a suitable desktop build.

## Known migration decisions requiring review

- **Maintainer decision (2026-09-15):** external reports exclude internal items; internal reports include them. Native Meeting Notes and Tracker builders implement this rule, and adapter-free report-builder coverage asserts both audiences.
- **Maintainer decision (2026-09-15):** every native report uses its selected report date consistently in its report content and generated subject. Adapter-free coverage uses non-today dates for Meeting Notes, Status, and Tracker reports.
- **Maintainer decision (2026-09-15):** imported prose should preserve safe rich-text formatting. Scripts, event handlers, and external-resource references remain inactive pending the G5 import-policy implementation; no legacy imported template is activated merely by this decision.
- Current tracker/status/meeting report collaboration calls pass `ignoreattachments=True`; Send Meeting Notes needs a separate context-attachment inventory.

## G1 status

**In progress / not passed.** The internal-item and selected-date decisions are now resolved and covered by native builders. The initial synthetic fixture and safe pure-builder capture exist, but legacy renderer page-layout metadata and the remaining migration inventories still need review. No Python removal or final report-parity claim is eligible.

## T01 verification

- `cmake -S . -B build-qmltests -DBUILD_QML_DESKTOP=OFF -DBUILD_QML_DESKTOP_TESTS=ON` passed.
- `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_communicationcontracts -j2` passed.
- `env QT_QPA_PLATFORM=offscreen build-qmltests/tests/desktop/tst_communicationcontracts -maxwarnings 0` passed: 6 passed, 0 failed. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` also passed.
- `cmake -S . -B build -DBUILD_QML_DESKTOP=ON -DBUILD_QML_DESKTOP_TESTS=OFF` and `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed.
- The Rendering target is intentionally contract-only in T01: the Linux Qt installation has no native WebEngine CMake package. No renderer capability is claimed; actual Qt WebEngine linkage remains T11/G4 work.

## T02 verification

- `tst_artifactstore` passed: profile/database settings isolation, immutable staged copy, duplicate names, traversal rejection, restart/cancelled recovery, corrupt manifest preservation, failed staging roots, manifest-owned cleanup, and destination-conflict/unwritable-destination staging retention (8 passed, 0 failed).
- `ctest --test-dir build-qmltests -R '^(communicationcontracts|artifactstore)$' --output-on-failure` passed: 2/2 tests.
- `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed after adding the ArtifactStore sources.
- No cleanup runs automatically and no staged operation is launched during recovery. Symlink behavior is covered on the current Linux host; platform-specific filesystem semantics must still be recorded when those platforms are tested.

## T03–T05 current verification

- `communicationrepository` passes against a throwaway SQLite schema, covering typed project/team/company/note loading, missing-company preservation, missing-project failure, selected-note ownership, and meeting-attendee provenance.
- `communicationcontracts` passes enum/options/fake-handoff, audience source bounds, override deduplication/role retention, recipient injection rejection, recipient-model role/manual/reset behavior, literal escaped field tokens, unsafe field-path rejection, and catalog/workflow-scoped field validation.
- `reportformatters` passes review-period and earned-value formatting cases. The focused T01/T02/T03/T05/T09 set passed with 4/4 CTest tests on 2026-09-12.
- `communicationcontracts` additionally exercises computed-field syntax/runtime/non-scalar/cancel/infinite-loop timeout behavior; no application or imported-template UI has been wired to this optional feature.
- `communicationcontracts` additionally covers template storage isolation, bundled/default precedence, user save, and idempotent legacy-definition import using a disposable settings file.
- `communicationcontracts` additionally covers context-specific Send Meeting Notes subject/body construction; `communicationrepository` verifies attendee IDs are retained on the selected note rather than only as a project-wide flag.
- `communicationcontracts` additionally covers mailto percent encoding, body/newline preservation, unsupported HTML rejection, header injection rejection, URL length preflight, and launch request outcome semantics.
- `communicationcontracts` additionally verifies Thunderbird compose grammar retains To/Cc/Bcc, Unicode/punctuation subject handling, and body/attachment file URLs through an injected no-launch seam.
- `communicationcontracts` additionally verifies EmailService backend selection, busy rejection, cancellation forwarding, operation identity, and completion lifecycle using a deferred fake backend.
- `communicationcontracts` additionally verifies audience-preset save/default lookup, project override precedence, and database isolation using disposable settings.
- `artifactstore` additionally verifies one-time legacy report-folder migration, including native-value precedence over PluginSettings.
- `communicationcontracts` additionally verifies native Status Report output uses EVM/review-period helpers, correct subject/stem, and escaped issue content.
- `communicationcontracts` additionally verifies project-wide Meeting Notes Report preparation carries a matching generation-bound source and native report document; `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed after the desktop project review route and its QML dialog were added. The focused `communicationcontracts` CTest and `git diff --check` also passed. No live email/client or report export was run.
- The same focused contract now verifies a Meeting Notes Report template resolves project/date fields and preserves the generated report in its protected content block. A new preparation lifecycle state clears a prior review before an asynchronous snapshot starts, so the desktop cannot display stale subject/body content while a project or note review is loading. `communicationcontracts` and the desktop `ProjectNotes` target passed after this change; delivery remains disabled in both native review dialogs.
- `communicationrepository` now verifies status/report-period and tracker/comment snapshot fields from a read-only fixture. `communicationcontracts` verifies typed Status and Tracker preparations map the snapshot to document content and their status-recipient/project-team audiences. The focused pair passed 2/2 and the desktop `ProjectNotes` target linked afterward.
- `communicationcontracts` additionally verifies native Tracker filtering, status grouping, priority ordering, internal exclusion, safe item rendering, and report subject convention.
- `communicationcontracts` additionally verifies ReportService dispatches each of the four native workflow inputs to a document.
- `communicationcontracts` additionally verifies report-to-request preparation preserves inline HTML, requires matching staged attachment formats, and keeps Do not email backend-free.
- Full current native regression suite: `ctest --test-dir build-qmltests --output-on-failure` passed 7/7 (`qmldesktop`, `filefinder`, `communicationcontracts`, `artifactstore`, `communicationrepository`, `microsoftoauth`, and `reportformatters`). Platform-specific renderer/client feasibility gates remain separate and unverified.
- `communicationrepository` additionally verifies async snapshot completion occurs on the repository owner thread, cancellation suppresses stale callbacks, and database-generation mismatches are rejected before querying. `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_communicationrepository -j2` and `ctest --test-dir build-qmltests -R '^communicationrepository$' --output-on-failure` passed (5 tests total in the executable); `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` also passed.
- `communicationcontracts` additionally verifies manual-recipient injection rejection, address-later state propagation/reset, and template-subject header-injection rejection. `ctest --test-dir build-qmltests -R '^(communicationcontracts|communicationrepository)$' --output-on-failure` passed (2/2); the desktop `ProjectNotes` build passed after the same changes.
- The same focused pair now covers duplicate-address role conflicts (with no implicit Bcc promotion) and HTML field escaping. The repository fixture covers manager/company request capture and transactional reads. The combined CTest command remained 2/2 green, and `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed.
- T13 extraction: `cmake -S . -B build-qmltests -DBUILD_QML_DESKTOP=OFF -DBUILD_QML_DESKTOP_TESTS=ON` and `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_filefinder -j2` passed. `env QT_QPA_PLATFORM=offscreen build-qmltests/tests/desktop/tst_filefinder -maxwarnings 0` passed (8/8), and a reconfigured desktop `ProjectNotes` build passed. These checks cover File Finder's existing behavior and linkage, not OAuth HTTP/profile-isolation behavior.
- T13 now has `tst_microsoftoauth`: it drives the device-code request through an injected fake transport and verifies the request/body and returned user code without a network call. It also verifies production (`tenant/client`) and developer-profile (`ProjectNotes-dev/tenant/client`) secret-key isolation. `ctest --test-dir build-qmltests -R '^(microsoftoauth|filefinder)$' --output-on-failure` passed 2/2; the desktop `ProjectNotes` build passed afterward.
- T13 refresh/sign-out baseline: `tst_microsoftoauth` now restores a synthetic saved credential, confirms explicit sign-out clears authenticated/in-progress state and removes the qualified secret, then covers `invalid_grant` removal and renewal status through the injected transport. `ctest --test-dir build-qmltests -R '^microsoftoauth$' --output-on-failure` passed on 2026-09-13.
- T14 scope baseline: `tst_microsoftoauth` verifies a caller-supplied `offline_access User.Read Mail.ReadWrite` request is preserved and has no `Mail.Send` or `Calendars` scope. It also verifies scope/configuration generation invalidation and access-token clearing. `ctest --test-dir build-qmltests -R '^microsoftoauth$' --output-on-failure` and the desktop `ProjectNotes` build passed.
- T15 presentation baseline: a successful synthetic Graph draft response carries its `webLink` through `EmailHandoffResult::presentationUrl`; the adapter exposes it as metadata only and performs no launch. Focused Graph/OAuth contracts passed 2/2 and the desktop `ProjectNotes` target rebuilt on 2026-09-13.
- T14 facade baseline: the same fake test restores a synthetic token, verifies `Office365Service` reports authenticated/generation state, injects its own Bearer header (overriding a caller-supplied Authorization header), and rejects an unauthenticated request before transport. The focused test and desktop build passed.
- T14 identity baseline: the facade’s fake `/me?$select=displayName,mail,userPrincipalName` result produces the account label `Ada Example <ada@example.test>` and is generation-guarded. `ctest --test-dir build-qmltests -R '^microsoftoauth$' --output-on-failure` passed.
- T15 draft baseline: `communicationcontracts` drives a synthetic OAuth refresh plus Graph draft creation through a scripted transport. It verifies the only email mutation is `POST /v1.0/me/messages`, bearer authorization is service-owned, no URL contains `send`, and the returned draft ID is retained. A second contract verifies a small operation-owned attachment becomes exactly one `/attachments` POST with the expected base64 data. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` and the desktop `ProjectNotes` build passed.
- T15 upload-session baseline: a synthetic attachment one byte over the simple-upload limit creates `/attachments/createUploadSession`, uploads 320 KiB chunks with exact `Content-Range` headers, and contains no Authorization header on the returned upload URL. The focused test and desktop build passed.
- T15 presentation-link baseline: Graph retains only HTTPS links on approved Outlook hosts; insecure or lookalike returned links are rejected from presentation metadata. The focused contract and desktop build passed on 2026-09-14; the backend still never launches retained links.
- T15 mail-scope baseline: Graph draft handoff now rejects an authenticated account without confirmed `Mail.ReadWrite` before issuing a draft POST. The synthetic contract verifies exactly the token-restoration request occurs for a File Finder-only scope, with no email mutation; focused tests and the desktop build passed on 2026-09-14.
- T15 cancellation baseline: a deferred fake Graph transport creates the draft, then cancellation occurs before its upload-session response. The adapter returns the uncertain `operation-cancelled` result and starts no upload PUT. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` passed on 2026-09-14. This is synthetic contract coverage only; Thunderbird and native platform/runtime verification remain explicitly deferred.
- T15 lost-create baseline: a synthetic network timeout after the draft POST returns `network-timeout` with uncertain outcome certainty, preserving the no-duplicate-create recovery path. The focused `communicationcontracts` test passed on 2026-09-14; no real mailbox request was made.
- T15 attachment-integrity baseline: a staged attachment modified before handoff is rejected with `attachment-integrity-mismatch` before any Graph draft POST. Valid small and large attachment fixtures now carry their recorded SHA-256 values, confirming the common preflight. The focused `communicationcontracts` test passed on 2026-09-14.
- T16 one-shot baseline: the mailto contract asserts RFC-style body newline and ampersand encoding, then drives both a successful and a failed injected URL opener. Either outcome records the operation as attempted; a second handoff returns `mailto-launch-already-attempted` without invoking the opener again. The focused `communicationcontracts` CTest passed on 2026-09-14, without launching an email client.
- T17 role-validation baseline: an invalid `RecipientRole` enum is rejected by `makeOutlookPrepareRequest()` with `recipient-role-invalid`; no malformed helper payload is emitted. The platform-neutral `communicationcontracts` CTest passed on 2026-09-14. Windows Outlook/COM verification remains held.
- G5 Qt rich-text baseline: `tst_richtextpolicy` uses `QTextDocument` only (with an offscreen Qt platform) to show that a round trip keeps bold/italic prose and removes script markup, but leaves an external image URL in its canonical HTML. The renderer's pure URL policy separately allows only its own `about:`/`data:` main-frame load and blocks every subresource, including remote/file/qrc/data/nested-frame URLs. The focused test passed on 2026-09-14. This prevents resource fetches during native rendering but is not a substitute for the imported-template normalization/review decision; no renderer runtime or resource request was performed.
- G5 import gate baseline: any template retaining a `legacyOriginal` import source now returns `template-import-review-required` before `TemplateApplication` renders subject or body. The legacy definition remains stored and visible for migration, but cannot enter native review/handoff until an explicit import-review/activation policy is implemented. The focused `communicationcontracts` CTest passed on 2026-09-14.
- T06 async-service baseline: `ComputedFieldService` schedules bounded evaluations off its owner thread, rejects a duplicate active operation ID, returns the result to the owner thread, and marks a cancelled active operation as cancelled. The focused `communicationcontracts` CTest passed on 2026-09-14.
- T10 tracker-filter baseline: the shared `ReportOptions` validator now rejects missing tracker types or statuses with specific diagnostics, so direct factory/service callers cannot silently turn an empty UI selection into a no-results report. The focused `communicationcontracts` CTest passed on 2026-09-14.
- Local regression baseline: after the T15 cancellation, lost-create, attachment-integrity, T16 one-shot, T17 protocol hardening, T06/G5 async/parser/resource/import-gate work, and T10 tracker-filter validation, `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j4`, `env CCACHE_DISABLE=1 cmake --build build-qmltests -j4`, and the full `ctest --test-dir build-qmltests --output-on-failure` suite passed 8/8 on 2026-09-14. `git diff --check` passed. Held Windows/macOS, Thunderbird, and native Qt WebEngine runtime/package checks were not run.
- Audience UI baseline: `RecipientSelectionPane.qml` binds to the C++ recipient model for selection, To/Cc/Bcc roles, manual additions, and explicit address-later state; all business validation remains in the model. The shipping QML compiler and desktop `ProjectNotes` link passed.
- T12 controller baseline: `communicationcontracts` uses the deferred backend to verify immutable preview-revision dispatch, Busy edit rejection, completion state/result retention, and no-email completion without backend invocation. The focused test and desktop build passed.
- T12 source-revalidation baseline: `communicationcontracts` verifies that revalidation freezes edits, returns source changes to the review with a diagnostic, can be cancelled back to review, and only dispatches after a successful guarded handoff. The desktop controller reloads and fingerprints the persisted source before entering that guarded handoff. `cmake --build build --target ProjectNotes -j2` and all 7 CTests passed on 2026-09-14.
- T12 source-gate baseline: after source revalidation fails, direct `CommunicationsController::handoff()` is rejected until replacement preparation is installed, preventing any API/QML caller from bypassing regeneration. The desktop build and focused contract test passed on 2026-09-14.
- T12 cancellation propagation baseline: cancelling a source-revalidation state emits an explicit controller signal; the desktop consumes it to cancel and clear the matching asynchronous repository request immediately. The focused controller contract asserts the signal, and the desktop build plus focused contract test passed on 2026-09-14.
- T17 protocol baseline: `communicationcontracts` round-trips version-1 Outlook helper prepare and presented-response frames, including operation identity, certainty, and opaque item handle. It derives a prepare payload from an immutable request while preserving recipient roles and staged attachment identity, and rejects header injection, invalid protocol versions, commands/stages, IDs, and oversized frames. The desktop build and focused contract test passed on 2026-09-14; this is transport-only and does not claim Windows COM validation.
- T19 platform-boundary baseline: `communicationcontracts` directly verifies the non-Apple Mac Mail adapter returns its specific unavailable result and preserves a pre-handoff cancellation. The desktop build and focused contract test passed on 2026-09-14; no macOS bridge behavior is claimed.
- T18 discovery baseline: `communicationcontracts` verifies a configured Thunderbird path is preferred when executable, ordered candidates fall back deterministically, and duplicate candidates are not re-probed. The desktop build and focused contract test passed on 2026-09-14; this does not claim discovery UI or sandbox-launch evidence.
- T12 cancellation baseline: the same contract registers an unrelated backend and verifies cancellation is delivered only to the backend that owns the active operation. The focused test passed.
- T12 desktop composition baseline: `tst_qmldesktop` rebuilt and linked with the application-lifetime controller exposed through `DesktopAppController`. Its UI-smoke sub-suite passed (12/12); the parity sub-suite could not mutate its existing SQLite fixture because it opened read-only (`attempt to write a readonly database`), before any communication action is exercised. The normal desktop `ProjectNotes` build remains the final link check for this slice.
- T09 rounding baseline: `reportformatters` now directly asserts legacy Python two-decimal behavior for 2.675 → 2.67, 2.685 → 2.69, 2.695 → 2.69, and -2.675 → -2.67. `ctest --test-dir build-qmltests -R '^reportformatters$' --output-on-failure` passed.
- T05 legacy-row baseline: `communicationcontracts` verifies repeated `[$projects.project_number.1]` substitutions and a distinct `[$projects.project_number.2]` resolve from typed one-based rows, with no template evaluation. The focused test passed.
- T05 malformed-path baseline: legacy expansion retains the compatibility fallback for a three-part token without row data, but an overlong token is unresolved rather than being silently truncated to a two-part lookup. The focused contracts, snapshot repository, and artifact-store CTests passed 3/3 on 2026-09-13.
- T03 fingerprint baseline: `communicationrepository` verifies the canonical snapshot fingerprint changes when note content changes and when the database generation changes. The focused test passed.
- T04 diagnostics baseline: `communicationcontracts` verifies an empty chosen-people source emits a non-blocking `audience-empty` diagnostic, and that diagnostic survives manual recipient editing in `RecipientSelectionModel`. The focused test passed.
- T04 source/company matrix baseline: the resolver contract covers current selection, selected-company filtering, unknown-company opt-in, project-manager exclusion, and missing managing-company setup failure, in addition to the existing status-recipient/client exclusion case. The focused test passed on 2026-09-14.
- T04 source-change baseline: replacing a recipient-model audience clears manual rows and an explicit address-later choice, so source changes cannot silently reuse the prior audience’s delivery exception. `communicationcontracts` passed on 2026-09-13.
- T04 preset-scope baseline: a project preset may be the matching project’s default but is rejected as a global or another project’s default. Focused contracts, snapshot repository, and artifact-store CTests passed 3/3; the desktop `ProjectNotes` target rebuilt on 2026-09-13.
- T21 preset baseline: native review dialogs now expose a C++-backed saved-audience picker plus global/project-scoped save control. The controller filters by workflow/project, resolves the saved rule against the immutable current snapshot, and reapplies only persisted-person overrides. `RecipientSelectionModel` contract coverage verifies those overrides mutate the selected role/state; focused contracts/repository/artifact CTests passed 3/3 and the desktop QML target rebuilt on 2026-09-13.
- T21 default baseline: the preset picker can make a selected eligible audience preset global or project-default. New native reviews load that default after their immutable snapshot and source are established, then resolve it in C++ and reapply persisted overrides. Focused contracts/repository/artifact/OAuth CTests passed 4/4 and the desktop QML target rebuilt on 2026-09-13.
- T20 inline-body baseline: `CommunicationsController` exposes editable review-local HTML only for inline mode, derives a plain preview, and rejects an edit for HTML/PDF attachment modes so a staged artifact cannot silently diverge. The review dialogs expose the editor only in inline mode. Handoff now retains controller-owned body and staged-attachment changes. Focused contracts/repository/artifact/OAuth CTests passed 4/4 and the desktop QML target rebuilt on 2026-09-13.
- T16 manual-output baseline: review exposes Save generated attachment only when an app-generated staged artifact exists. `DesktopAppController` selects that artifact by generated identity and delegates to `ArtifactStore::publish` after the user chooses a Save-file destination; it rejects user-supplied attachments and does not launch a client. Generated-attachment controller coverage, focused contracts/repository/artifact/OAuth CTests (4/4), and the desktop QML build passed on 2026-09-13.
- T16 one-shot baseline: `MailtoEmailBackend` marks a platform-launch attempt before calling its opener and rejects a second handoff for that operation with an uncertain already-attempted result. The contract confirms only one opener call; focused tests and the desktop build passed on 2026-09-14.
- Controller/UI-state baseline: `CommunicationsController` exposes QML-safe Busy/revision/stage/diagnostic properties and invokable handoff/cancel, with contract assertions for reviewing, preparing-backend, and completed state names. `RecipientSelectionModel` exposes selected/available counts and its reset is QML-invokable. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` passed; the normal desktop `ProjectNotes` build compiled the corresponding QML pane.
- Desktop snapshot-lifecycle baseline: the controller creates an `AsyncSqliteCommunicationRepository` only after its database is open, advances the database generation, and destroys the repository (joining its worker) before closing that database. `ctest --test-dir build-qmltests -R '^(communicationrepository|communicationcontracts)$' --output-on-failure` passed 2/2; `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed.
- Meeting-note review baseline: `MeetingNotesPreparationFactory` requires matching project/database-generation source context, builds the typed Send Meeting Notes document, resolves the full project-team default (including the manager), and produces an immutable review preparation. `DesktopAppController::prepareMeetingNotesReview()` uses its read-only repository to populate that preparation and the shared recipient model without rendering, launching, or handing off. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` passed; the desktop `ProjectNotes` build passed.
- Final-recipient baseline: `DesktopAppController::handoffPreparedReview()` derives recipients/address-later from `RecipientSelectionModel` immediately before dispatch. Invalid recipient resolution is retained by `CommunicationsController` as failed review state rather than reaching a backend; the contract covers that failure/recovery transition. The focused contract test and desktop build passed.
- Native review UI baseline: Project Note Detail saves the note before invoking `prepareMeetingNotesReview()` and opens `MeetingNotesReviewDialog`. The dialog uses the shared recipient/controller objects, shows pending/failure state, and deliberately has no delivery activation while adapters/settings are not integrated. The shipping QML compiler and `ProjectNotes` desktop build passed.
- Review-content baseline: `CommunicationsController` exposes the generated subject and plain-text body as QML-safe review properties; the dialog displays a bounded message preview. The controller contract and desktop `ProjectNotes` build passed.
- Subject-edit baseline: the review controller rejects CR/LF subject injection, retains an edited subject through final request rebuilding, and exposes a QML-invokable setter used by the review dialog. The focused contract and desktop build passed.
- Review-state guard baseline: subject edits are rejected before a preparation exists or while a handoff is active; the focused contract verifies the empty-controller rejection and passed.
- Template field-catalog baseline: `templateFieldPaths(workflow)` exposes only catalog fields applicable to that workflow for a future Insert Field UI. The contract verifies `meeting.date` is available to Send Meeting Notes but excluded from Status Report and passed.
- Template-editor baseline: `TemplateEditorModel` is the QML-facing adapter over `CommunicationTemplateStore`; it scopes settings by profile/database, inserts only workflow-applicable literal field tokens, persists an optional plain-text alternative alongside rich prose, and validates via `TemplateParser` before saving. The Email Templates settings pane binds only to that model. `communicationcontracts` verifies field applicability, save/reopen, duplicate/reset, plain-body persistence, and database isolation; `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` and the normal desktop `ProjectNotes` build passed. The combined QML runner's existing parity fixture remains read-only before UI actions, so that runner is not evidence for this panel's runtime interaction.
- Protected-template baseline: `TemplateApplication` treats `{{ content.body }}` as the one protected native-content insertion, requires it exactly once in non-empty rich/plain custom bodies, escapes normal template values, and never evaluates template code. The meeting-notes factory can apply a saved matching template while preserving its native HTML and plain-text content; the review dialog selects a saved template and rebuilds from the captured immutable snapshot while retaining recipient edits. Fields unavailable from that snapshot are rejected into review validation rather than silently substituted with an empty value. Contracts cover escaping, missing/duplicate protected-block rejection, per-mode body output, factory integration, and unavailable-context diagnostics. Focused contracts, the QML desktop build, and the shipping desktop build passed; no email delivery is activated by this path.
- Bundled-template baseline: `TemplateEditorModel` now exposes the resettable `send-meeting-notes-native-v1` template as its Send Meeting Notes default. A user override with that stable id replaces it; Reset removes the override and restores the native wrapper. The focused template-editor contract asserts the bundled baseline as well as its database isolation.
- Recipient-remove baseline: `RecipientSelectionModel::removeManual()` deletes only a review-local manual address; source-derived recipients cannot be deleted by this UI path. The shared pane renders the control only for manual entries. The recipient contract covers both allowed and rejected removal; the desktop target built afterward.
- Backend-preference baseline: the Template/Email Settings pane now stores a preferred Mailto/Thunderbird/Graph backend and a Thunderbird executable path through `EmailSettingsStore`, scoped to the active profile. The selected backend is captured into the immutable meeting-note preparation; no backend is registered by the desktop composition root, so this remains a retained preference rather than a claim that the current inline-HTML review can launch/deliver. Focused contracts and the shipping desktop build passed.
- QML smoke follow-up: the Email Templates field-insertion dialog now has a fixed width, avoiding its `implicitWidth` binding loop, and the Settings smoke expectation includes the ninth Email Templates tab. `tst_qmldesktop`'s `UiSmokeTest` passed 12/12 with no runtime QML warnings. The encompassing `qmldesktop` CTest target remains failed only because its separate parity fixture opens the SQLite database read-only before UI actions (`attempt to write a readonly database`; 21 cascading parity failures). `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` and focused `communicationcontracts`/`communicationrepository` CTest both passed.
- T23 coexistence baseline: table-scoped plugin menus now suppress only the three exact bundled native-report callbacks (`exportnotes_plugin.py`, `exportstatusreport_plugin.py`, and `exporttrackeritems_plugin.py`). Matching is by module filename plus callback identity, so unrelated bundled Settings entries and any custom plugin actions remain visible. The QML test target and normal desktop target rebuilt, and focused `communicationcontracts`/`communicationrepository` CTest remained 2/2 green. The full QML CTest still has the separately documented read-only parity fixture failure.
- Report email-mode baseline: Status and Tracker review factories retain `ReportOptions.emailMode` instead of overwriting it with inline HTML. The project-report review dialog supplies the selected mode to validated controller entry points for all three report workflows; the existing three-argument methods retain the inline-HTML default for callers that have not adopted the selector. Contract coverage asserts HTML-attachment and no-email mode retention. `communicationcontracts` passed, both the QML test target and shipping desktop target rebuilt, and `git diff --check` passed. Attachment/PDF generation and client handoff remain governed by the renderer/backend gates and are not claimed by this review-only UI.
- Tracker-options baseline: the Tracker Items review now exposes the native default type/status filters and passes the selected type/status sets through a validated controller entry point to `ReportOptions.tracker`; empty type/status selections are rejected before snapshot work begins. This uses the existing native `TrackerItemsReportBuilder` filtering rather than duplicating it in QML. `communicationcontracts` passed and the shipping desktop target rebuilt. Display/retain-artifact options remain unavailable until renderer/artifact orchestration exists.
- Project-report template baseline: Status and Tracker reports now have resettable native wrapper defaults and can safely apply a workflow-matching user template around their protected generated content. The preparation factory supplies project/report date/internal/type context, escapes ordinary field values, and rejects a workflow mismatch. The controller retains the immutable snapshot/source/options needed to rebuild a templated project report while preserving review-local recipient edits. The shared report dialog selects templates for all three report workflows. Contracts cover Status template application and bundled Status template visibility; `communicationcontracts` and the shipping desktop target passed. Template values without snapshot support still produce review validation rather than an empty substitution.
- Audience-refinement baseline: native review dialogs now expose project-team, meeting-attendee, and status-recipient shortcuts plus All/Managing Company/Project Client/Except Client filters, unknown-company opt-in, and manager exclusion. The controls pass stable values to `DesktopAppController`, which holds only the current immutable review snapshot and invokes `resolveAudience`; QML does not filter people itself. The default shortcut mirrors each route’s native default. A new action clears the retained audience snapshot before loading, preventing an earlier review from being reused. The shipping desktop target rebuilt successfully; resolver behavior remains covered by the focused contracts.
- Attachment-staging baseline: every native review now creates a manifest-owned operation. HTML-attachment mode stages its standalone generated document; either review dialog can also choose a file, which `ArtifactStore::stageUserAttachment` copies into that operation before `CommunicationsController` lists it. The backend therefore never receives an arbitrary live filesystem path. Staging failures become review validation, and the controller contract verifies attachment tracking plus busy-state rejection. PDF mode now reserves an operation-owned path and waits for the native renderer before it replaces review state; runtime/package validation remains deferred under G4. Focused `communicationcontracts` and `artifactstore` CTest passed 2/2; the shipping desktop target rebuilt and `git diff --check` passed.
- QML test-harness recovery: Qt test mode resolved the desktop fixture under a sandbox-read-only `~/.qttest` path, causing the earlier parity-only SQLite failures. The runner now establishes unique writable `/tmp` XDG data/config roots before `QApplication` and does not enable Qt test mode. `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_qmldesktop -j2` followed by `ctest --test-dir build-qmltests -R '^qmldesktop$' --output-on-failure` passed; the complete `ctest --test-dir build-qmltests --output-on-failure` run passed 7/7 on 2026-09-13. This is automated Linux evidence only; it does not satisfy renderer or client-platform gates.
- T06 runner baseline: duplicate enabled computed-field paths are rejected before evaluating the duplicate expression, avoiding order-dependent replacement. The computed-field contract also cancels an expression while it is executing, rather than only before it begins. `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` passed; `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j2` passed and `git diff --check` was clean on 2026-09-13.
- T07 audience references were removed from email templates. Audience presets are configured only during recipient review, where they apply to a concrete project context.
- T22 audience-reference control: Settings now exposes a workflow-matched list of global presets from the active database. The template stores only the selected opaque ID; project-scoped presets are intentionally omitted because a reusable template has no project context, and the form states that applying recipients remains an explicit review action. The template-editor contract verifies this ID survives a close/reopen cycle. Native Qt WebEngine runtime, Thunderbird, Windows, and macOS verification remain deferred by maintainer direction.
- T06 structured-context baseline: computed expressions now receive both the documented read-only nested form (`context.project.name`) and the existing flat-key compatibility form (`context['project.name']`). Context data is copied into each fresh engine, deep-frozen, and rejected above the defined input bound. Contracts cover structured lookup, attempted mutation, cancellation while a loop is executing, and input/output/definition bounds. `communicationcontracts`, the shipping desktop build, and `git diff --check` passed on 2026-09-13.
- T07 legacy-definition baseline: legacy dynamic Email/Meeting definitions now preserve their original Type, Name, Invitees, Subject, Template, and Data Type in a profile/database-scoped review-only ledger. Settings names these records as retained for later migration; they never activate calendar commands. Only a legacy Email definition whose Data Type is `project_notes` receives the unambiguous Send Meeting Notes mapping. The template contracts cover idempotence and preservation of an unsupported Meeting/people definition; `qmldesktop` and `communicationcontracts` passed on 2026-09-13.
- T07 duplicate-legacy baseline: imported legacy identities now include canonical-row occurrence, preserving otherwise identical source rows separately while a repeated import remains idempotent. The importer retains compatibility with a first-occurrence ID written by the prior content-only scheme. The focused contract and desktop build passed on 2026-09-14.
- T07 incomplete-draft baseline: content/template storage accepts incomplete prose, unresolved workflow fields, and an empty subject so authors can save work for later repair. Preview/application still report those issues and block handoff; CR/LF subject injection is still rejected at storage. Contracts cover direct store persistence and editor save/reopen/duplicate/reset with an incomplete draft. `communicationcontracts` passed on 2026-09-13.
- T07 corrupt-store baseline: a syntactically malformed nonempty template setting is now diagnosed as `template-store-corrupt`, preserves bundled defaults for read-only fallback, and refuses a subsequent save rather than overwriting user state. The focused contract and desktop build passed on 2026-09-14.
- T06 function-form baseline: computed fields accept the documented `function compute(context, helpers)` declaration as well as the existing scalar-expression form. `helpers` is deliberately a frozen empty object until individually reviewed pure helpers are added, so no hidden application API is exposed. The function-form contract passed on 2026-09-14.
- T06 engineering-limits baseline: contracts now assert the architecture’s 64-definition, 64 KiB expression/output, and 1 MiB UTF-8 context ceilings. The runner also enforces 250 ms per field and 2 s total evaluation budget. The focused suite and desktop build passed on 2026-09-14.
- T14 granted-scope baseline: `Office365Service::account()` now reports only scopes returned by the successful OAuth token response, rather than treating requested consent as proof of grant. Confirmed scope names persist separately from refresh/access tokens under a profile-qualified File Finder settings key, restore into a new OAuth-manager lifetime, and clear on scope/configuration changes, sign-out, and failed refresh. Synthetic OAuth/facade coverage verifies exact persistence/restoration and removal at sign-out; focused File Finder/OAuth tests passed on 2026-09-14. Serialized multi-capability consent remains.
- T14 consent-serialization baseline: a second device-code sign-in request while the first is pending is ignored, and refresh restoration is guarded as single-flight until its callback completes or a state-changing action invalidates it. The synthetic device-code test verifies one HTTP request after two sign-in calls; `microsoftoauth` passed on 2026-09-14.
- T24 Widgets coexistence baseline: a fresh Linux-only `BUILD_QML_DESKTOP=OFF`, `BUILD_WIDGETS_LEGACY=ON` configuration with `/usr/bin/c++` configured and built `ProjectNotesWidgets` successfully. The default ccache wrapper could not configure in this sandbox because its cache was read-only; that environment artifact was avoided without changing product build settings. Widgets still compiles the Python plugin manager and is not a native-report bridge. **Maintainer scope decision (2026-09-14): Widgets is no longer supported; make no further Widgets changes and do not require a native bridge for release-scope cleanup.**
- Platform-gate hold (2026-09-14): continue native source/local automated work, but hold Windows and macOS validation, Thunderbird testing, and native Qt WebEngine runtime/package verification until separately resumed. These checks remain unverified rather than unsupported or passed.
- T02 operation-collision baseline: `ArtifactStore::createOperation()` now rejects an existing operation directory instead of overwriting its manifest. The focused regression stages an artifact, retries with the same UUID, and verifies recovery retains the original project and artifact. `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j4` and `ctest --test-dir build-qmltests -R '^artifactstore$' --output-on-failure` passed on 2026-09-14. The native Qt WebEngine runtime remains an explicit return item under G4; no browser/runtime attempt was made in this container.
- T03 selected-attendee baseline: snapshot loading now starts with the project team and adds non-team people only from the selected meeting notes; it no longer leaks attendee candidates from other project meetings. The attendee relation joins active people, so a soft-deleted person is absent from both the candidate list and note provenance. The focused fixture covers an external attendee on an unselected second note plus a deleted attendee on the selected note. `ctest --test-dir build-qmltests -R '^communicationrepository$' --output-on-failure` and `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j4` passed on 2026-09-14.
- T03 invalid-date baseline: invalid stored note epochs now produce an invalid `QDateTime`, rather than silently becoming `1970-01-01`. The repository fixture confirms this typed missing/invalid distinction; `ctest --test-dir build-qmltests -R '^communicationrepository$' --output-on-failure` passed on 2026-09-14.
- T03/T04 current-selection baseline: `CommunicationSnapshot` now retains `currentSelectionIds` for selected-note provenance and a separate `currentSelectionPersonIds` for audience resolution. The repository captures the project-team context for meeting/tracker workflows and only receive-status members for Status Report before it adds non-team attendees; the resolver uses the person list. Focused repository and communication-contract CTests passed on 2026-09-14.
- T03/T04 team-provenance baseline: `SnapshotPerson` now marks actual project-team membership independently of status and meeting-attendee provenance. Project Team resolution requires that marker, while selected external attendees remain eligible only through attendee/chosen sources. Contracts assert that an attendee-only person is not added to a Project Team resolution. Focused `communicationcontracts`/`communicationrepository` CTests and the shipping desktop build passed on 2026-09-14.
- T04 manual-address baseline: `RecipientSelectionModel::addManual()` now rejects invalid roles and case-insensitive duplicates already present in the selected audience or added manually, preventing misleading duplicate counts and silent resolution collapse. The focused communication-contract CTest passed on 2026-09-14.
- T02 generated-artifact idempotency baseline: `registerGeneratedFile()` rejects a path already present in the operation manifest, so a repeated async producer callback cannot duplicate an attachment. The focused artifact-store CTest passed on 2026-09-14.
- T02 generated-path normalization baseline: generated-file registration cleans the candidate path before checking that it is below the operation’s artifact directory. The focused test rejects a lexical `artifacts/../manifest.json` path, preventing an internal manifest file from being registered as an attachment; artifact-store CTest passed on 2026-09-14.
- T02 partial-manifest baseline: recovery now has a focused fixture for a syntactically valid manifest missing required workflow fields. It is diagnosed as corrupt and renamed to `manifest.corrupt.json`, alongside malformed JSON, rather than appearing as recoverable state; artifact-store CTest passed on 2026-09-14.
- Local regression baseline: after the T02/T03/T04 hardening, `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j4` and the full `ctest --test-dir build-qmltests --output-on-failure` suite passed 7/7 on 2026-09-14. This is local automated evidence only and does not replace the explicitly deferred native Qt WebEngine runtime/package checks under G4.
- T03 writer-lock baseline: the asynchronous repository test acquires the shared writer lock before scheduling a snapshot and verifies no completion occurs until that lock is released. This demonstrates the worker observes the same database lock convention as writers; focused `communicationrepository` CTest passed on 2026-09-14.
- T05 optional-field baseline: rendering a catalog-marked optional field with no context value now produces empty output without an unresolved-field diagnostic; required missing fields remain rejected. The focused communication-contract CTest passed on 2026-09-14.
- T06 Unicode-output baseline: computed-field output accounting now uses serialized UTF-8 byte length, matching the public limit contract. A result of repeated non-ASCII characters that fits below the old UTF-16 code-unit threshold but exceeds the byte cap is rejected; the focused communication-contract CTest passed on 2026-09-14.
- T07 preserved-legacy corruption baseline: malformed nonempty preserved-definition JSON now produces `legacy-template-store-corrupt` and leaves the original setting untouched when import is attempted. The focused communication-contract CTest passed on 2026-09-14.
- T02 publication-integrity baseline: generated artifact publication now requires a regular staged file whose size and SHA-256 still match its reviewed metadata. The focused fixture modifies a staged file after registration and verifies the copy is rejected as `publish-source-invalid`; artifact-store CTest passed on 2026-09-14.
- T25 static native-path audit: refreshed source-only searches over `ProjectNotesEmail`, `ProjectNotesEmailRendering`, and `ProjectNotesIntegrations` found no `Mail.Send`, `sendMail`, calendar/event endpoint, Python embedding, plugin-manager, or `.py` reference. The only Graph mutations remain draft creation under `/v1.0/me/messages` and its attachment endpoints; `/me` identity lookup is read-only. This is a narrow static check, not backend/platform behavior evidence.
- T20 no-email completion baseline: project-report review now exposes **Complete without email** only after the user explicitly chooses the no-email mode. It invokes the existing source-revalidation/handoff state machine, whose `EmailContentBuilder` completes `EmailMode::None` before selecting a backend. The visible completion state makes the safe no-account/no-client route usable without enabling an email backend.
- T04 matrix extension: the audience resolver contract now asserts normal Managing Company, Project Client, and Except Project Client intersections, as well as chosen-person source filtering with project-manager exclusion. This closes the positive-path coverage that the earlier empty/unknown-company scenarios did not exercise.
- T03 project-wide meeting provenance: a Meeting Notes Report request without selected note IDs now treats every project note as selected for attendee/action loading. The repository contract uses two meetings with different attendees and note-bound actions to prove that report content no longer loses this provenance.
- T03 external-attendee provenance: snapshot people now union saved project-team members with non-deleted attendees of that project's non-deleted notes. A regression fixture verifies an attendee outside the team appears in the report note's attendee list while retaining no team-only status-recipient flag.
- T25 Linux regression rerun: after the T03 snapshot fixes, `ctest --test-dir build-qmltests --output-on-failure` passed all 7 registered desktop tests (`qmldesktop`, File Finder, communication contracts, artifact store, communication repository, Microsoft OAuth, and report formatters). This remains automated Linux evidence only; it does not replace the explicitly deferred renderer/package/client checks.
- T15 production-transport baseline: `QtNetworkHttpTransport` now supplies the production implementation of the token-free `HttpTransport` seam, preserving exact request methods, headers, bodies, response headers/status/body, timeout diagnostics, and stale-operation suppression after cancellation. It does not add consent, create drafts, or activate a client. The desktop target and the focused CTest registration build on 2026-09-14. Its loopback integration assertions are explicitly skipped in this sandbox because `QTcpServer` cannot bind a localhost listener; `ctest -R '^httptransport$'` therefore passes with two skips. Run those assertions on a normal local host before treating the transport as runtime-verified. Held Windows/macOS, Thunderbird, and Qt WebEngine runtime/package gates remain unrun.
- Local regression baseline: after adding the transport target, `env CCACHE_DISABLE=1 cmake --build build-qmltests -j4` and `ctest --test-dir build-qmltests --output-on-failure` passed all 9 registered tests on 2026-09-14. The new `httptransport` CTest is green because its two real loopback checks are skipped under this sandbox restriction; it is not production HTTP evidence.
- T13/T15 composition baseline: the desktop composition root now uses File Finder's shared token-free `Office365Service`, supplies that facade with the production Qt HTTP transport, and registers the draft-only Graph adapter with the application-lifetime `EmailService`. QML still has no email-delivery action, so construction alone cannot create a draft, open a client, request consent, or send mail. The shipping `ProjectNotes` target and the real `tst_qmldesktop` smoke/parity CTest built and passed on 2026-09-14. Canonical Office settings ownership, feature-consent UI, and final reviewed-handoff UI remain T13/T14/T20/T23 work.
- T13 shared-service ownership baseline: `Office365Service` now owns its production `MicrosoftOAuthManager` and proxies authentication/status/configuration for File Finder, so File Finder no longer directly calls the OAuth type. It receives only the service's internal worker-token capability; that capability is not a QML property or invokable method. The service is the sole canonical writer for tenant/client values and mirrors the historic PluginSettings keys during migration. A temporary on-disk settings regression proves later root/rule saves retain those values. `tst_filefinder` and `tst_microsoftoauth` passed, followed by the full local 9/9 CTest suite and shipping desktop build on 2026-09-14. Feature-scoped consent, account-switch behavior, and QML setup remain incomplete.
- T14 token-free setup-model baseline: `Office365SettingsModel` now exposes the shared service's tenant/client, authentication state, device-code URL/code, account label, and sign-in/out commands to the Office 365 Settings tab. It delegates legacy tenant/client and File Finder enablement through the compatibility service, never exposes an access/refresh token, and keeps the File Finder tab's scanner option separate. The shipping desktop target and `qmldesktop` CTest passed on 2026-09-14.
- T14 capability-consent baseline: the Office 365 settings facade now offers an explicit **Enable email drafts** action. It requests exactly `offline_access`, `User.Read`, and `Mail.ReadWrite`; it never asks for `Mail.Send`, calendar, Teams, or Files scopes. Confirmed draft scopes unlock identity lookup through the token-free service, while File Finder remains the default sign-in capability. A scope change preserves an authenticated File Finder access token until a successful interactive Microsoft response explicitly confirms the new scopes; background refreshes cannot satisfy or clear that pending consent. The focused `microsoftoauth` CTest verifies a valid File Finder session remains authenticated while draft consent is initiated, remains authenticated after a synthetic device-code denial, and emits a device-code request containing only the draft scope bundle. It passed on 2026-09-14. No real Microsoft account, mailbox, or platform email-client test was performed.
- T14 identity-failure baseline: `/me` responses are generation-guarded and now retain the last verified label if the transport/status fails, emitting only a diagnostic; an empty identity response cannot make a draft route appear verified. The focused OAuth test first records `Ada Example <ada@example.test>`, injects a synthetic `/me` failure, and verifies that label is not overwritten. The focused CTest and shipping desktop target passed on 2026-09-14.
- Local regression baseline: after the capability-consent, identity-failure, and Graph-review wiring work, `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j1`, `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_qmldesktop -j4`, and `ctest --test-dir build-qmltests --output-on-failure` passed all 9 registered tests on 2026-09-14. `git diff --check` passed. The initial full-suite attempt found only an absent QML-test executable after a tool-observation interruption; rebuilding that target completed normally before the successful rerun. Held Windows/macOS, Thunderbird, native Qt WebEngine runtime/package, and real Microsoft-account checks were not run.
- T04 eligibility/count baseline: audience resolution now rejects malformed addresses before they can inflate the review list or unique recipient count, and `Everyone except the client` returns `company-setup-required` when the project client is not configured rather than treating every known company as eligible. The recipient model’s displayed candidate/selected counts deduplicate case-insensitive addresses, matching final delivery semantics. Focused communication-contract coverage verifies both setup/invalid-address cases and duplicate count behavior; `ctest --test-dir build-qmltests -R '^communicationcontracts$' --output-on-failure` passed on 2026-09-14.
- T12/T15 account-generation baseline: `EmailPreparation` now captures the selected Graph account label and session generation when review staging begins, and `EmailContentBuilder` copies both values into its immutable `EmailRequest`. This makes Graph’s existing account-generation preflight effective: a review created for the current account can proceed, while a sign-out, tenant/client change, or renewed consent before handoff is rejected rather than creating a draft under a changed account. The focused contract asserts the transfer and a mismatched generation fails before any second Graph request; it passed on 2026-09-14.
- T15 presentation baseline: completed review state now exposes the Graph draft identity and its already-validated Outlook presentation URL. Native review dialogs show **Open draft in Outlook** only after a completed result supplies that URL; it is a user click and never an automatic launch. Controller contracts cover result propagation, while QML smoke coverage loads the new bindings without warnings. `ctest --test-dir build-qmltests -R '^(communicationcontracts|qmldesktop)$' --output-on-failure` passed on 2026-09-14. No real Outlook/browser action was invoked.
- T20 result-language baseline: native review dialogs now state **Microsoft 365 draft created** only when the completed handoff contains a Graph draft identity, keeping the result distinct from an unconfirmed composer/URL launch. The QML build and full local CTest suite passed 9/9 on 2026-09-15; no external presentation action was invoked.
- T20/T23 Graph handoff baseline: the native meeting-note and project-report review dialogs expose **Create Microsoft 365 draft** only when Graph is the selected backend, `Mail.ReadWrite`/`User.Read` consent is confirmed, and the token-free `/me` lookup supplied an account label. The action reuses the guarded source-revalidation/controller handoff path and can only create a draft through the already-registered Graph adapter; it has no send path. All other client backends remain unavailable in these dialogs. `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j1`, `ctest --test-dir build-qmltests -R '^(qmldesktop|microsoftoauth)$' --output-on-failure`, and the full local `ctest --test-dir build-qmltests --output-on-failure` suite passed 9/9 on 2026-09-14. These are synthetic/Linux checks only; Windows, macOS, Thunderbird, native Qt WebEngine runtime/package, and real Microsoft-account validation remain intentionally held.
- T21 audience-explanation baseline: `resolveAudience()` now retains each noneligible snapshot person with a stable resolver reason instead of forcing QML to reproduce audience logic. `RecipientSelectionModel` exposes those translated reasons, source provenance, stable company grouping, and unique selected To/Cc/Bcc counts. Its visual sorting is explicitly reversed before recipient resolution, so delivery keeps the original resolver order. `RecipientSelectionPane.qml` renders grouped eligible rows plus a collapsible excluded-contact explanation. The new contract verifies exclusions, role counts, and order preservation; `ctest --test-dir build-qmltests --output-on-failure -R 'communicationcontracts|qmldesktop'` passed 2/2 on 2026-09-15. This is Linux/QML smoke evidence only.
- T21 internal-audience baseline: the C++ recipient model receives only an internal-report flag and the immutable snapshot’s managing-company ID, then warns when selected unique recipient addresses fall outside that company (or when the company setup is missing). The shared QML pane displays this independently from report content/options; recipient changes immediately recalculate it. The contract covers both external-recipient and missing-setup warnings. Focused `communicationcontracts`/`qmldesktop` CTests passed 2/2, the shipping `ProjectNotes` target built with `CCACHE_DISABLE=1`, and `git diff --check` passed on 2026-09-15.
- T21 selected-company baseline: the existing `SelectedCompanies` resolver branch is now reachable from the shared picker. `DesktopAppController` returns only immutable-snapshot company IDs, labels, and counts; QML sends selected IDs back through one advanced rule invokable, with all eligibility intersection remaining in C++. The picker provides a company checklist only for that refinement. The QML target rebuilt with `CCACHE_DISABLE=1`, focused `communicationcontracts`/`qmldesktop` CTests passed 2/2, and `git diff --check` passed on 2026-09-15. An ordinary rebuild can fail in this environment because ccache’s configured cache is read-only; this is not a source/CMake failure.
- T21 captured-choice baseline: the audience picker now exposes **Current selection** and **Choose people**. The controller supplies only immutable-snapshot person IDs/labels/addresses/company labels, and the advanced rule entry point supplies selected person IDs to the existing `CurrentSelection`/`ChosenPeople` resolver branches; QML does not form recipients or filter them. The QML target rebuilt with `CCACHE_DISABLE=1`, focused `communicationcontracts`/`qmldesktop` CTests passed 2/2, and `git diff --check` passed on 2026-09-15. Search/filter ergonomics and full UI scenario coverage remain.
- T21 picker-search baseline: the eligible-recipient list and Choose-people checklist now have local text filters over visible snapshot labels/address/company/provenance. Search hides rows only; it never changes selected IDs, resolver inputs, role counts, or recipient resolution. `tst_qmldesktop` rebuilt and focused `communicationcontracts`/`qmldesktop` CTests passed 2/2 with `git diff --check` on 2026-09-15.
- T03 project-folder baseline: the transactional, worker-owned snapshot now captures one optional local report root from `project_locations`: a non-deleted `File Folder` with deterministic preference for the `Project Folder` description. The captured path participates in the immutable fingerprint, but no renderer/UI/filesystem action consumes it yet. Repository fixtures prove preferred/live selection over another folder and a deleted candidate; focused `communicationrepository`/`communicationcontracts` CTests passed 2/2 and `git diff --check` passed on 2026-09-15.
- T02/T07 export-default baseline: `EmailSettingsStore` now returns the documented native defaults when neither a database-specific nor global value exists: Issues List, Status Reports, and Meeting Minutes under Project Management. Explicit scoped/global values, including an explicit empty override, still win; Send Meeting Notes has no report folder default. `tst_artifactstore` asserts all defaults and focused `artifactstore`/`communicationrepository` CTests passed 2/2 with `git diff --check` on 2026-09-15.
- T11 publication-target baseline: `ReportDestination` now resolves a captured local project root, configured relative export subfolder, and report filename without writing files. It rejects URLs/non-absolute roots, traversal, absolute/drive-qualified subfolders, and unsafe filenames before `ArtifactStore::publish` is ever called. Artifact tests cover valid resolution and each rejection; `tst_artifactstore` passed with `git diff --check` on 2026-09-15. Creating destination folders, hash-checked publication, and UI option wiring remain.
- T27 native-help baseline: the in-app native review guide now documents Current selection, Choose people, selected-company refinement, grouped reason/count/exclusion display, and internal-report audience mismatch notices. It corrects stale wording: local-client routes remain uncertified, but a separately consented and identity-verified Microsoft 365 route can create a draft only; optional Outlook presentation is a user click and neither action sends mail or creates calendar items. Legacy help and the deferred WebEngine/client-certification scope remain explicit. Documentation-only change; no platform/client/runtime action was run.
- T20/T11 retained-HTML staging baseline: `EmailPreparation` now carries the explicit retain-HTML option through Status, Tracker, and Meeting Notes preparation (including template rebuilds). When enabled, desktop staging retains a manifest-owned standalone HTML artifact even for inline, no-email, or PDF modes; it does not publish automatically and no user-facing option is wired yet. `communicationcontracts` passed, `ProjectNotes` configured and built with `CCACHE_DISABLE=1`, and `git diff --check` passed on 2026-09-15. Windows/macOS, Thunderbird, and native Qt WebEngine runtime/package validation remain held.
- T11/T20 retained-HTML publication baseline: the project-report review now exposes **Generate HTML report** and captures that request through the immutable preparation path for Meeting Notes, Status, and Tracker reports. Its manifest-owned HTML is atomically copied to the captured local project folder plus the database-scoped export subfolder. Directory materialization rejects a missing/symlinked project root, symlinked path components, and canonical escapes; a destination failure remains a review warning while preserving the staged artifact. Artifact-store coverage creates/publishes the HTML through nested directories and rejects symlink escape; focused `artifactstore`, `communicationcontracts`, and `qmldesktop` CTests passed 3/3 on 2026-09-15. The shipping target also built with `CCACHE_DISABLE=1`. Native Qt WebEngine runtime/package and held platform/client validation were not run.
- T11 page-layout propagation baseline: `RenderRequest` now carries the prepared report's declared `QPageLayout`; the WebEngine renderer uses it instead of forcing A4 portrait. Contracts assert the Status Letter-portrait/20 mm and Tracker Letter-landscape/12 mm layouts, and the rendering + shipping targets built with `CCACHE_DISABLE=1`; focused `communicationcontracts` passed on 2026-09-15. This is source/build evidence only: PDF rendering and package/runtime checks remain explicitly held.
- T11/T20 display-PDF source baseline: **Display report when complete** now reaches the immutable project-report preparation for every report workflow. It forces operation-owned PDF staging even when the chosen email mode is inline HTML or no-email, but attaches that PDF only for the explicit PDF-attachment mode; the requested desktop presentation is a best-effort post-staging action and a failure is retained as a visible nonfatal diagnostic. Staging warnings are now propagated through asynchronous review setup instead of being dropped. QML smoke and communication-contract CTests passed 2/2 and `ProjectNotes` built with `CCACHE_DISABLE=1` on 2026-09-15. No WebEngine/PDF or desktop-presentation runtime action was invoked; those checks remain held.
- T07 native-report-settings migration baseline: after the QML desktop database opens, it imports the three historical `ProjectNotes[profile]/PluginSettings` export-subfolder values into only missing native report settings. The imported values are scoped to the SHA-256 database identity, preserve existing database-specific and profile-wide native choices, and leave the Python/plugin store unchanged. `tst_qmldesktop` rebuilt serially with `CCACHE_DISABLE=1`, `ctest --test-dir build-qmltests -R '^qmldesktop$' --output-on-failure` passed, and `git diff --check` was clean on 2026-09-15. The added profile-wide-precedence regression passes in `artifactstore` serially. Held client/platform/runtime checks were not run.
- T08/T10 adapter-free builder baseline: `tst_reportbuilders` now isolates native Meeting Notes, Meeting Notes Report, Status, and Tracker document checks from the wider communication-contract target, which also contains held client-adapter cases. It verifies rich note/action/attendee preservation, report-date/internal filtering, user-value escaping, EVM output, tracker filtering/sort order, subjects/stems, and each declared page layout. `cmake -S . -B build-qmltests`, serial `tst_reportbuilders` build, and `ctest --test-dir build-qmltests -R '^reportbuilders$' --output-on-failure` passed on 2026-09-15. It does not start an email client, WebEngine, or a renderer process.
- T04 adapter-free audience baseline: `tst_audienceresolver` now isolates the C++ source/company intersection matrix and recipient-selection model from client-adapter contracts. It covers managing/client/except-client/selected-company/unknown-company behavior, current selection and chosen people, project-manager exclusion, malformed addresses, role/deduplication conflicts, manual entries, address-later reset, exclusion explanations, internal-report disclosure, and source-order preservation. Serial configure/build and `ctest --test-dir build-qmltests -R '^audienceresolver$' --output-on-failure` passed on 2026-09-15; no adapter, browser, renderer, or network path was initialized.
- T05/T06/T07 focused safe-contract rerun: exact Qt Test selection ran the literal field/parser/legacy-expansion, bounded/asynchronous computed-field, template-store/editor, and protected-content application cases in `tst_communicationcontracts`. All 8 selected cases passed on 2026-09-15. The invocation named only those functions; it did not invoke Thunderbird, mailto, Graph, Outlook, Mac Mail, WebEngine, or network test cases.
- T15 focused synthetic-draft rerun: exact Qt Test selection ran the six Graph-draft cases covering draft-only request construction, uncertain POST outcome retention, staged-file integrity, simple/large attachment upload, and pre-upload cancellation. All six passed on 2026-09-15 against the in-memory HTTP transport. No Microsoft account, network request, send endpoint, Thunderbird path, browser, or WebEngine process was invoked.
- T12 focused state-machine rerun: exact Qt Test selection ran fake-handoff operation-identity, active-operation/stale-completion, immutable email-mode, and no-email completion cases. All four selected cases passed on 2026-09-15. The fakes record requests locally; no backend executable, account, browser, renderer, or network path was exercised.
- T13/T14 synthetic OAuth rerun: the standalone `microsoftoauth` CTest passed serially on 2026-09-15. Its test executable injects `FakeHttpTransport` and temporary settings/secret callbacks, covering device-code single-flight, profile-qualified secrets, explicit draft-only scopes, File Finder session preservation during draft consent, token-free authorization, identity settings ownership, and credential clearing. It made no real OAuth, account, browser, or network request.
- T03 repository rerun: the standalone `communicationrepository` CTest rebuilt and passed serially on 2026-09-15. It exercises the worker-owned read-only SQLite snapshot fixture and does not initialize an email adapter, browser, renderer, or external client.
- Held-gate-safe local regression: `env CCACHE_DISABLE=1 cmake --build build-qmltests -j1` and `ctest --test-dir build-qmltests --output-on-failure -E '^communicationcontracts$'` passed 10/10 on 2026-09-15, followed by clean `git diff --check`. The excluded combined contract executable is the only registered suite containing Thunderbird cases; its safe parser/template, state-machine, and Graph functions were run separately by exact name. The opt-in WebEngine renderer target remains unregistered and no Windows/macOS/client/runtime gate was exercised.
- T16 manual-review baseline: both native review dialogs now offer an explicit **Copy plain-text message** action alongside the existing manifest-owned **Save generated attachment** flow. The controller copies only the current reviewed plain text after a user click and never stages, launches, or sends a client request. The QML desktop target rebuilt serially and `qmldesktop` passed on 2026-09-15. Its first post-build deployment attempt found a stale generated test-plugin copy that CMake could not remove; only that exact `build-qmltests/tests/desktop/plugins` output directory was removed after inspection, then CMake recreated it and the target/test passed. No source plugin, user setting, client, renderer, or platform test was touched.
- T10 tracker-order parity baseline: legacy source confirms each status group is ordered High/Medium/Low, then by parseable due date descending, with undated items last and stable input order for equal values. `tst_reportbuilders` now asserts the same-priority later/earlier/undated sequence; it rebuilt and passed serially on 2026-09-15 without a renderer or client.
- T16 mailto-unit rerun: the exact `encodesAndBoundsMailtoRequests` contract case passed on 2026-09-15. Its URL opener is injected; no desktop URL opener or mail client was called.
- T09 review-period boundary baseline: native `reviewPeriodDates` follows the legacy Monthly/Weekly/Bi-Weekly rules. `tst_reportformatters` now covers non-leap and leap month-end clamping, both period endpoints, Bi-Weekly, unknown/None frequencies, and invalid dates; it rebuilt and passed serially on 2026-09-15 with no renderer or client dependency.
- T09 EVM guard baseline: `tst_reportformatters` now asserts legacy-compatible zero/negative actual-cost behavior, zero earned value, missing/malformed numeric inputs, and literal display-value preservation. The formatter must return `No Schedule` only where its formula guard requires it and still calculate safe independent metrics. The serial target build and focused CTest passed on 2026-09-15.
- Held-gate-safe local regression rerun: after the manual-review and report-parity changes, serial `cmake --build build-qmltests -j1` followed by `ctest --test-dir build-qmltests --output-on-failure -E '^communicationcontracts$'` again passed all 10 eligible tests on 2026-09-15, with clean `git diff --check`. Thunderbird remains excluded, and no WebEngine runtime, Windows, macOS, or client test was run.
- T07/T22 workflow-template selection fix: changing template workflow previously retained the old workflow’s selected ID, so the editor could show an empty draft instead of the new workflow’s native wrapper. `TemplateEditorModel` now clears that workflow-scoped selection before reload. A new exact-function contract verifies all four workflows expose their resettable native `{{ content.body }}` wrapper; it and the existing editor test passed serially, and `qmldesktop` passed after relinking on 2026-09-15. No client, renderer, or platform gate was exercised.
- T07/T22 database-template selection fix: switching a template editor from a database containing a custom template to a clean database could likewise retain an unavailable ID and display an empty draft. Database switching now clears the selection before reload, so the new scope selects its bundled baseline. The exact workflow/database/editor test selection passed serially on 2026-09-15; no client, renderer, or platform gate was exercised.
- G1 decision regression: report-builder coverage now asserts external Meeting Notes omit internal notes while internal reports include them, and asserts the selected non-today reporting date in Meeting Notes, Status, and Tracker subjects/content. Meeting Notes and Status now also retain that selected date in the legacy-compatible report footer. `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_reportbuilders -j1`, its focused CTest, and the shipping `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j1` build passed on 2026-09-15; no email client, Thunderbird, WebEngine renderer, Windows, or macOS check was invoked. `git diff --check` also passed.
- T10 internal-status correction: `StatusIssue` now retains the source tracker item’s internal flag through project-report preparation. The Status builder excludes such issues from external reports and retains them for internal reports; `tst_reportbuilders` asserts both cases. `env CCACHE_DISABLE=1 cmake --build build-qmltests --target tst_reportbuilders -j1`, its focused CTest, and the shipping `env CCACHE_DISABLE=1 cmake --build build --target ProjectNotes -j1` build passed on 2026-09-15. No client, renderer, or held platform gate ran.
- T10 status-issue parity correction: native Status preparation now selects only open (`New`/`Assigned`) `Tracker` items, matching the legacy exporter rather than its previous non-existent `Issue` type. The builder keeps the legacy stable order: High/Medium/Low priority, then parseable due date descending, with undated/unknown values last. The exact `preparesStatusAndTrackerReportsFromSnapshot` and `buildsStatusReportWithEvmAndEscapedIssues` contract functions passed serially on 2026-09-15, alongside the adapter-free report-builder CTest. Thunderbird, client, renderer, Windows, and macOS cases were not invoked.
- T10 empty-issues formatting correction: Status output now preserves the legacy empty Issues table instead of adding a new “No issues.” row. The adapter-free report-builder test covers that empty result and passed serially on 2026-09-15; no client, renderer, or held platform test ran.
- T10 tracker internal-column correction: internal Tracker reports now render the legacy `Y`/`N` values rather than `Yes`/`No`; the internal-row assertion, focused report-builder CTest, and serial `ProjectNotes` build passed on 2026-09-15. No client, renderer, or held platform check ran.
- T08 Meeting Notes structure correction: the project-wide builder now has the legacy report title, meeting-block/table classes, conditional action-item header, selected-date footer, and branding rather than reusing the distinct Send Meeting Notes generic table wrapper. The adapter-free report-builder fixture asserts these structural markers along with rich note content and audience/date filtering; its serial CTest passed on 2026-09-15. Renderer and held platform/client checks were not run.
- T09/T10 branding parity correction: Status and Tracker output now retain the legacy Project Notes branding line after their selected-date footer. The adapter-free builder fixture asserts it for both reports; the serial `reportbuilders` CTest passed on 2026-09-15. No renderer or held platform/client check ran.
- Held-gate-safe local regression after report parity work: `env CCACHE_DISABLE=1 cmake --build build-qmltests -j1` and `ctest --test-dir build-qmltests --output-on-failure -E '^communicationcontracts$'` passed all 10 eligible CTests on 2026-09-15. The combined contract executable was built but not run because it contains the held Thunderbird case; no Thunderbird, WebEngine runtime, Windows, macOS, or external-client test ran. `git diff --check` passed.
- T25 native-route source audit: `prepareProjectReportReview()` captures a read-only snapshot, builds typed options/documents, and stages only generated artifacts before entering review; the report-review route has no Python invocation or email-service handoff. A repository-wide native-source search found transport `send()` calls only in the explicit OAuth/Graph transport/service boundary, not in report builders or native review preparation. This is static source evidence, not a live delivery claim.
- T25 Graph endpoint audit: `GraphEmailBackend` creates a draft only with `POST https://graph.microsoft.com/v1.0/me/messages`, then uses its attachment endpoints (`/attachments` or `/attachments/createUploadSession`) as required. Source inspection found no Graph `/send`, calendar, or invitation endpoint in the native adapter. The exact fake-transport `createsGraphDraftWithoutSend` contract passed on 2026-09-15, recording only in-memory requests; no account, network, or browser request was made.
- T12 state-machine fixture update: the guarded-handoff/no-email contract now explicitly selects address-later for its recipient-free fake review, matching immutable request validation. The exact `coordinatesPreparationHandoffAndNoEmail` contract passed serially on 2026-09-15, covering source-revalidation freeze, stale-edit rejection, deferred backend completion, and no-email completion without a backend handoff. No client, network, renderer, or Thunderbird path ran.
- Safe contract regression rerun: exact literal parser/legacy expansion, bounded/asynchronous computed-field, template-store/protected-content, state-machine/no-email, and fake Graph-draft functions passed 9/9 on 2026-09-15. The invocation named only those functions and did not invoke Thunderbird, mailto, Outlook, Mac Mail, WebEngine, a live client, browser, network, Windows, or macOS path. `git diff --check` passed.
- T20 report-dialog viewport correction: the bounded native report-review dialog now wraps its full options/review body in a vertical `ScrollView`, so long tracker/audience/review content remains reachable rather than extending below the window. `tst_qmldesktop` rebuilt serially and its focused CTest passed on 2026-09-15; no report renderer, client, Thunderbird, or platform runtime test ran.
